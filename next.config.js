/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: [
      'www.logodesign.net',
      '1h3.googleusercontent.com'
  ],
  },
}

module.exports = nextConfig
