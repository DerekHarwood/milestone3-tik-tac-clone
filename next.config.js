/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: [
      'www.logodesign.net',
      'lh3.googleusercontent.com'
    ],
  },
}

module.exports = nextConfig
